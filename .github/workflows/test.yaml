name: Express.js CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

  workflow_dispatch:
env:
  SPEECH2TEXT_API_KEY: ${{secret.SPEECH2TEXT_API_KEY}}
  SPEECH2TEXT_SERVICE_URL: ${{secret.SPEECH2TEXT_SERVICE_URL}}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - run: npm ci
      - run: npm test
      
